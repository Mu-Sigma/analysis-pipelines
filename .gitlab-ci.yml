image:
  name: foundationbricks/foundation_brick_test
  entrypoint: [""]

stages:
  - updateVersion
  - pushFiles

runUpdateVersion:
  stage: updateVersion
  script:
    - cd support
    - python3 UpdateBricksVersion.py
    
runPushFiles:
  stage: pushFiles
  script: 
    - ls -al